Gate 2 long paragraph fixture starts here and intentionally stays as one paragraph so line breaking and page breaking can be reviewed from a deterministic source: JWord renders each page on its own canvas, keeps only visible pages plus buffer pages alive, releases offscreen canvases to 1x1, records layout and render timings separately, avoids a single long canvas, and leaves PDF, DOCX, hit-test, rect mapping, header, footer, table, and image work to later gates while still giving Gate 2 a stable long-text input that can be used by visual baselines and render benchmarks without browser services.
