# Gate 5 PDF Font Fixtures

`NotoSansSC-gate5-subset.ttf` is a small Google Fonts `Noto Sans SC` subset for the text `中文PDF导出`.

It is used only by tests proving configured PDF fonts can cover Chinese text without falling back to PDF standard fonts. Keep this fixture subset small and do not replace it with a full CJK font file.
